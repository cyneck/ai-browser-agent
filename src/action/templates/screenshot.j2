# 截取屏幕截图
try:
    screenshot_bytes = page.screenshot()
    screenshot_base64 = to_base64(screenshot_bytes)
    result = {
        "success": True,
        "message": "成功截取屏幕截图",
        "screenshot": screenshot_base64
    }
except Exception as e:
    result = {
        "success": False,
        "message": "截图失败",
        "error": str(e)
    }